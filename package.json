{
  "name": "prestoposti",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build src/index.ts --outdir ./build"
  },
  "dependencies": {
    "hono": "^4.8.3"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}